---
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    cooldown:
      default-days: 10
    groups:
      ci-dependencies:
        patterns:
          - "*"
    commit-message:
      prefix: "[ci]"
    labels:
      - maintenance
